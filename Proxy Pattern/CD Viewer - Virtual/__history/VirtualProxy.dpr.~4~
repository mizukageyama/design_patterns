program VirtualProxy;

uses
  Vcl.Forms,
  CDFrm in 'CDFrm.pas' {CDForm},
  ImageLoader in 'ImageLoader.pas',
  ImageLoaderIntf in 'ImageLoaderIntf.pas',
  ImageLoaderProxy in 'ImageLoaderProxy.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TCDForm, CDForm);
  Application.Run;
end.
